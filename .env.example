# Clerk Authentication
# Get these from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here
CLERK_SECRET_KEY=your_clerk_secret_key_here

# Base URL
# Used for generating absolute URLs (e.g., in sitemap, Open Graph tags)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Google Analytics (Optional)
# Get your Measurement ID from https://analytics.google.com
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=

# Cloudflare Configuration
# These are configured in wrangler.jsonc, but you can override them here for local development
# D1 Database will use --local flag for development
# R2 Buckets will need to be configured in wrangler.jsonc

# Admin Emails
# Comma-separated list of email addresses that have admin access
# Example: ADMIN_EMAILS=admin@example.com,user@example.com
ADMIN_EMAILS=
